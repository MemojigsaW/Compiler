

void foo();

int main() {
	for (;;) {
		int x = 3 * 4;
		int y = -3 + 4 * 5;
		foo(6) ? 7 : (8);
		int d = -x;
		d = d - (-3);
		d = d -2;
		d = d-y;
		float q = x+(-3);
		if (x>=1)
			int a = 20;
		if ( d == 5){
			z = x+y;
			}
		return 0;

	}
}
